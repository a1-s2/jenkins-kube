apiVersion: v2
kind: Pod
metadata:
  name: myweb-pod2
  labels:
    env: testing
    region: IN
spec:
  replicas: 3
  selector:
    matchLabels:
       env: production
  #  matchExpression:
   #   - { key: env, operatorL in, values: [ production] }
   #   - { key: region, operatorLin, values: [IN] }
  template:
    metadata:
     name: myweb-pod
     labels:
       env:production
       dc: "2"
       region: IN
     spec:
        containers:
        - name: myweb-con
           image: vimal
  containers:/apache-webserver-php
  - name:myweb-con
    imageL httpd
